# M16: 测试时计算扩展（Test-Time Compute）

## 为什么学
- 这是理解“多想一会儿为什么可能更准、但也更贵”的关键模块。

## 前置依赖
- M10 解码策略
- 基本推理与采样直觉

## 诊断题
1. self-consistency 为什么可能提升正确率？代价是什么？
2. 什么时候“多想一会儿”会适得其反？

## 核心知识点
- `核心必会`：self-consistency、反思、tree search 等方法的共同点。
- `核心必会`：测试时计算与训练时能力的边界。
- `工程重点`：延迟、token 消耗、不稳定性等成本。

## 易错点 / 边界条件
- 容易把“更多推理步数”误当成稳定收益。
- 容易忽略任务类型和验证机制对收益的决定作用。

## 练习
- [ ] 比较“增加 test-time compute”和“换更大模型”的取舍。

## 费曼输出
- 用 120 字解释：为什么 test-time compute 不是免费的性能提升？

## 完成标准
- 诊断题正确率达到 80%。
- 完成 1 次费曼输出。
- 完成 1 个取舍比较题。

## 后续关联
- 与 `M10` 相连：test-time compute 可视为更高层的解码 / 搜索扩展。
