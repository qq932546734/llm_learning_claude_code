# M07: 指令微调（Instruction Tuning）

## 为什么学
- 它是从“会续写”到“会当助手”的关键桥梁。

## 前置依赖
- M02 预训练目标
- M01 基本模型结构直觉

## 诊断题
1. 为什么少量高质量指令数据也能显著改变助手行为？
2. SFT 容易导致哪些副作用（遗忘、风格漂移、过拟合）？
3. LoRA 为什么能省显存？它限制了什么表达能力？

## 核心知识点
- `核心必会`：SFT 调整的是输出行为分布，而非彻底重建知识。
- `核心必会`：数据格式、多样性和难度覆盖比“量大”更重要。
- `工程重点`：LoRA / QLoRA / DoRA 是不同的参数高效微调路径。

## 易错点 / 边界条件
- 容易把“指令遵循变好了”误解为“知识更多了”。
- 容易忽略数据分布导致的风格偏置和遗忘。

## 练习
- [ ] 给一个客服助手场景，设计 5 条高质量 SFT 数据样例。

## 费曼输出
- 用 120 字解释：SFT 改变的到底是什么？

## 完成标准
- 诊断题正确率达到 80%。
- 完成 1 次费曼输出。
- 完成 1 个数据设计练习。

## 后续关联
- 与 `M08` 相连：SFT 是 RLHF / DPO 的起点模型和行为基线。
