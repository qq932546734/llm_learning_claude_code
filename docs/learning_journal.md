# 学习日志（History）

> 本文件存放历史学习记录；当前状态面板统一查看 `docs/progress.md`。

## 2026-04-01
- 记录类型：提问模式盘点 / 课程资产整理（不计入主线新学，不触发新增主题复习）
- 模块：`跨模块（M01-M16）`
- 学习内容：提问模式下对 `modules/` 做一次全局体检，逐个阅读 `M01-M16`，并对照 `curriculum/` 与 `docs/` 中已有学习资产，判断当前课程的知识面覆盖与模块深度是否足够
- 诊断表现：当前课程主干覆盖已经较完整，适合“面试准备 + 基础巩固”；但若目标是更完整的现代 `LLM` 知识图谱，主线之外仍需扩展主题 `E01` ~ `E06`：`E01` 评估与基准测试、`E02` 安全与红队、`E03` 数据工程、`E04` RAG 与检索、`E05` 量化与服务栈、`E06` 现代架构扩展。深度上 `M01 / M03 / M06 / M07 / M08 / M12` 明显更实，而 `M09 / M13 / M14 / M15 / M16` 在 `modules/` 文件中仍偏提纲级；另外，部分模块的真实学习深度已经沉淀在 `docs/learning_journal.md` 与 `docs/question_analysis.md`，但尚未充分反写回模块正文，导致“实际掌握深度 > modules 表面深度”
- 提问：新增 `Q085`
- 模块：`跨模块（M01-M16）`
- 学习内容：继续在提问模式下明确“后续补哪些、怎么补、如何不打断当前学习与复习节奏”，并将补强策略落成“主线冻结、旧复习优先、新内容延后激活”的执行原则
- 诊断表现：补强策略已明确为两层：第一层是课程资产回写，即把已经学过但只沉淀在 `docs/learning_journal.md` / `docs/question_analysis.md` 的深度反写回 `modules/`，这类工作不占你的主线学习时间；第二层才是新主题扩展，默认放到 `Week 4` 之后的延长期或 `Day 7` 弹性日。这样可以保护当前 `M13 -> M15 -> M16 -> Week 4 面试化收束` 主线不被打断，也不会提前给旧复习队列叠加新的间隔复习压力
- 提问：新增 `Q086`
- 模块：`跨模块（M01-M16）`
- 学习内容：按“静默回写优先”策略，完成第一批课程资产整理，把 `M09 / M13 / M14 / M15 / M16` 从提纲级补到“面试+选型级”，统一补齐主线定义、边界、1 个工程 trade-off、1 组高频问法与 1 个场景题
- 诊断表现：本次回写不改变当前 `Week 3 / Week 4` 主线，也不新增正式复习压力；作用是把原本只散落在 `docs/learning_journal.md` / `docs/question_analysis.md` 中的深度，回收到 `modules/` 正文，降低“实际掌握深度 > 模块表面深度”的结构性失衡。其中 `M09 / M13 / M14` 主要基于近期真实学习记录完成回写，`M15 / M16` 则补齐到与当前课程目标一致的“面试+选型级”基线
- 提问：无新增问题（本次为课程资产整理）
- 模块：`M08` 人类反馈强化学习（RLHF） / `M03` 分词与表示
- 学习内容：学习模式下先用 `Q078` 回抽“`RLHF-PPO` 里的 `KL penalty` 不是直接加进 `clip loss`，而是先在 reward 侧做 shaping，再流向 `return / advantage / clip loss`”，随后用 `Q031` 回抽“为什么 `WordPiece` 更像似然导向训练准则，而 `Unigram` 更像显式概率模型”
- 诊断表现：当前已能较稳说出 `KL penalty -> shaped reward -> return -> advantage -> clip loss` 这条主线，但仍需继续避免把 `advantage` 简化成“reward 减平均值”；`M03` 方面则已能区分“训练准则层”与“显式概率模型层”，也已纠正“自然导向”这类术语漂移
- 提问：无新增知识库问题（本次为热身回顾）
- 模块：`M13` 多模态 LLM
- 学习内容：正式补完 `Week 3 / Day 2` 的遗留内容，连续完成“多模态 instruction tuning 到底在教什么、为什么不只是会看图”“为什么多模态评估不能只看最后答对”“遮图后准确率只小幅下降说明什么”三条核心题，并额外做了 1 轮 1-2 句短口述压缩
- 诊断表现：当前已能把核心口径压成“基于视觉证据 + 按指令回答”，也能稳定说出“答对不等于真的用了图，评估还要看视觉证据依赖”；同时已能把 `语言先验 / 数据偏置 / 视觉证据依赖` 串成工程化解释，并能用遮图、关键区域扰动、反事实替换举例。当前仍需继续压稳一点：把“为什么不能只看答对”进一步压成更短的 2 句面试答案
- 提问：无新增知识库问题（本次为主线收束与短口述压缩）
- 模块：`M14` Agent 与工具使用
- 学习内容：在“报销单审核助手”的 `M13 + M14` 迁移题中，再次判断 `OCR / 报销政策检索 / 写入财务系统` 的工具边界，继续回顾“什么时候不该让模型自由选择工具”
- 诊断表现：当前已能较稳判断信息获取类工具更适合模型参与决策，而 `写入财务系统` 这类状态修改动作不该自由执行；“不该自由选工具”的判据已重新固定到“高风险 / 高成本 / 强权限约束”
- 提问：无新增知识库问题（本次为迁移题中的边界回顾）
- 模块：`M03` 分词与表示
- 学习内容：继续完成 `tied embeddings`、`tokenizer 为什么同时影响 prefill / decode` 与 `embedding` 参数量估算，显式计算 `50k x 4096 = 204,800,000`，并区分 `tied ≈ 205M`、`untied ≈ 410M`
- 诊断表现：当前已能较稳说出 `tied embeddings` 省参数、也常顺带省显存与带宽，但牺牲输出层的独立表达自由度；同时也已能把 tokenizer 的影响拆成“序列长度影响 `prefill + KV cache`”和“词表大小影响输出层 / logits 计算”两条线。当前仍需继续压稳一点：把 tokenizer trade-off 再压成更短的面试口径
- 提问：无新增知识库问题（本次为配套复习与估算题）
- 模块：`M15` 模型可解释性 / `M01` Transformer 架构深度解析
- 学习内容：已在会话末确认重新对齐到 `Week 3 / Day 3`，下次将从 `M15` 主线正式开场，并先完成 `M01` 的 `Attention` 完整公式与 `LayerNorm` 动机复习
- 诊断表现：`Week 3 / Day 2` 已视为补完，后续不再卡在 `M13` 遗留；下次开场直接进入 `Day 3`
- 提问：无新增知识库问题（本次为主线切换确认）

## 2026-03-31
- 模块：`M08` 人类反馈强化学习（RLHF）
- 学习内容：开场先用 `Q078` 热身回顾“`RLHF-PPO` 里的 `KL penalty` 不是直接加进 `clip loss`，而是先在 reward 侧做 shaping，再流向 `return / advantage / clip loss`”
- 诊断表现：当前已能较稳说出 `KL penalty` 的放置主线，但仍需继续压稳两点：一是把 `shaped reward -> return -> advantage -> clip loss` 说得更顺；二是继续避免把 `advantage` 简化成“reward 减平均收益”
- 提问：无新增知识库问题（本次为热身回顾）
- 模块：`M13` 多模态 LLM
- 学习内容：先完成 `Week 3 / Day 1` 的收束，依次重答并压稳“`vision encoder -> projector -> LLM` 的标准结构”“为什么多模态不是图片转文字再给普通 `LLM`”“为什么多模态输入通常先恶化 `TTFT`”“为什么会看图不等于会基于视觉证据按指令回答”；随后完成 1 道 `M13 + M14` 迁移题，围绕“报销单照片 -> OCR / 检索 / 系统写入”判断什么时候需要工具、什么时候不该自由选工具；在此基础上正式进入 `M13`（下），继续推进“多模态 instruction tuning 教的是什么”“为什么评估不能只看答对”“遮图后准确率下降很少说明什么”“为什么图片问答准确率高不足以单独证明系统设计得好”这一组下半部分核心题，并完成 1 轮 3 题短收束
- 诊断表现：当前已能较稳口述 `vision encoder` 提视觉特征、`projector` 做跨模态桥接与对齐、`LLM` 做统一的理解/推理/生成，也已能说出多模态常先恶化 `TTFT` 的主因是“前置视觉计算 + 更多视觉 token 带来的更重 `prefill`”；任务层面，已能把“多模态 instruction tuning”固定成“基于视觉证据按指令回答”，也已理解“答对不等于真的用了图”，并能用“语言先验 / 数据偏置 / 遮图后性能变化”来解释为什么多模态评估不能只看结果是否正确。当前仍需继续压稳三点：一是不要把 `projector` 说成“把图片变成文本 token”；二是继续把“证据使用能力 + 指令遵循能力”的联合目标压成更短口径；三是把“不能只看答对”继续压成更工程化的评估表达，如遮图、关键区域扰动、反事实验证
- 提问：无新增知识库问题（本次以主线收束、短口述与评估边界推进为主）
- 模块：`M14` Agent 与工具使用
- 学习内容：在 `M13 + M14` 迁移题中再次回顾“什么时候不该让模型自由选择工具”，并把判据重新压稳到“`高风险 / 高成本 / 强权限约束`”
- 诊断表现：当前已能在报销单图片场景下把 `OCR / 检索 / 系统写入` 分层清楚，也已能较稳补全 `强权限约束` 这一层；后续仍适合继续补 1 道 observation 异常后的 `re-plan` 题
- 提问：无新增知识库问题（本次为迁移题中的边界回顾）
- 模块：`M03` 分词与表示
- 学习内容：作为 `Week 3 / Day 2` 的配套预热，完成“`embedding` 是什么”“为什么大 `vocab` 不一定更好”“为什么 tokenizer 不只是预处理细节、会同时影响 `prefill / decode`”“WordPiece 为什么更像似然导向训练准则、Unigram 为什么更像显式概率模型”“`tied embeddings` 省了什么、牺牲了什么自由度”这一组短口述题
- 诊断表现：当前已能较稳把 `embedding` 定位成“离散 token id 到连续向量表示的映射”，也能较稳回答“序列长度成本 vs 词表维度成本”的 tokenizer trade-off；`WordPiece / Unigram` 的主干也已通。当前仍需继续压稳三点：一是 `tied embeddings` 要避免说成“必然损伤性能”，更稳的口径应是“减少输出层自由度，但常是可接受的工程折中”；二是补 1 轮 `embedding` 参数量估算；三是继续把 tokenizer 对 `prefill / decode / TTFT / TPOT` 的影响压成更短面试口径
- 提问：无新增知识库问题（本次以预热承接与短口述为主）

## 2026-03-30
- 模块：`M08` 人类反馈强化学习（RLHF） / `M03` 分词与表示
- 学习内容：按 `Week 3 / Day 1` 开场先用 `Q078` 热身回顾“`RLHF-PPO` 里的 `KL penalty` 不是直接加进 `clip loss`，而是先在 reward 侧做 shaping，再流向 `return / advantage / clip loss`”，再用 `Q030` 回顾“`tokenizer` 是上位概念，`BPE / WordPiece / Unigram` 是 tokenizer 内部可采用的并列方法”
- 诊断表现：当前已能较稳说出 `KL penalty` 的放置层级，也能较稳区分“上位概念 vs 具体方法”；仍需继续压稳两点：一是把“`KL penalty` 先形成 shaped reward、再算 return”说得更自然；二是继续减少 `M03` 术语漂移，避免把 `WordPiece` 写错或把 tokenizer 与方法并列
- 提问：无新增知识库问题（本次为热身回顾）
- 模块：`M13` 多模态 LLM
- 学习内容：正式启动 `M13`（上），连续完成“为什么多模态 LLM 不能简单理解成‘图片先转文字再交给普通 LLM’”“`vision encoder -> projector -> LLM` 的标准结构”“为什么视觉 token 与文本 token 是接口兼容而非本体相同”“为什么多模态输入会引入前置视觉计算与更重的 `prefill`、从而先恶化 `TTFT`”“为什么常见训练路线不是从零联合训练三部分，而是先复用 backbone、优先训练 `projector`”“为什么多模态 instruction tuning 本质上是在学会基于视觉证据按指令回答”“为什么多模态评估不能只看答对，还要看是否真的依赖视觉证据”这一组主线题
- 诊断表现：当前已能较稳口述 `vision encoder` 负责提视觉特征、`projector` 负责桥接与对齐、`LLM` 负责统一的理解/推理/生成，也已能说出多模态系统不只增加一段视觉前向，还会因为更多视觉 token 拉长上下文，使 `prefill` 更重、`TTFT` 更差；训练层面已理解 `projector` 往往先训，因为它是跨模态接口、也是常见随机初始化的新参数；任务层面已开始固定“多模态 instruction tuning = 基于视觉证据按指令回答”的统一口径。当前仍需继续压稳三点：一是不要把“映射到 LLM 可消费的表示空间”误说成“变成文本 token”；二是把“会看图 != 会按图回答”再压成更短的 2-3 句面试答案；三是补 1 道“看图 + 调工具”的跨模块迁移题，把 `M13` 与 `M14` 串起来
- 提问：无新增知识库问题（本次以新主线推进、短口述和层级纠偏为主）
- 模块：`M14` Agent 与工具使用
- 学习内容：按 `Week 3 / Day 1` 带 1 轮边界复习，回顾“什么时候不该让模型自由选择工具”，重新压稳判据应固定成“`高风险 / 高成本 / 强权限约束`”
- 诊断表现：当前已能说出 `高风险 / 高成本` 两层，但仍需要继续把 `强权限约束` 稳定补全，避免回落成只从模型能力角度判断是否该自由选工具
- 提问：无新增知识库问题（本次为到期边界回顾）

## 2026-03-28
- 模块：`M12` 生产部署 / `M14` Agent 与工具使用
- 学习内容：按 `Week 2 / Day 6` 综合日先用 `Q084` 热身回顾 `continuous batching` 的最小调度粒度，重新压稳“它更接近 decode step 边界上的动态补位，而不是整任务级 batching”；随后继续把 `M12` 与 `M14` 串起来，围绕“为什么 agent latency 不能只看 LLM latency”“为什么多轮 `plan -> act -> observe -> re-plan` 会把 `TTFT / TPOT / tool latency / 往返轮数` 累加起来”完成口述与综合迁移题
- 诊断表现：当前已能较稳说出 agent 总延迟更像“多轮 LLM 调用 + 多轮工具延迟 + observation 带来的额外轮次”的串联累计，也能继续保持 `continuous batching = step 级动态补位` 的口径；当前仍需继续压稳两点：一是把 `TTFT / TPOT` 作为“单轮 LLM 调用内部拆分”的层次说得更自然，二是继续补 1 道 observation 异常后的 `re-plan` 迁移题，把“重试 / 换源 / 降级回复”压成标准动作
- 提问：无新增知识库问题（本次以综合串讲、标准口径压缩与系统迁移题为主）
- 模块：`M03` 分词与表示
- 学习内容：完成“为什么 tokenizer 不只是预处理细节”“为什么不能简单说大 `vocab` 一定更好”“大 `vocab` 往往如何把成本从 `prefill` 挪到 `decode`”三条主线，并完成 1 道“长输入 + 多轮 agent”场景下 tokenizer 取舍的综合题
- 诊断表现：当前已能较稳串起 `粒度 -> 平均序列长度 -> prefill / KV cache / TTFT / TPOT` 与 `词表大小 -> embedding / output projection / logits 开销` 两条线，也能在 agent 场景里做条件化判断：若长输入下 `TTFT` 是主要痛点，会倾向优先考虑更短序列方案；当前仍需继续压稳两点：一是把“大 `vocab` vs 小 `vocab`”的 trade-off 压成 2-3 句更短的面试答案，二是继续减少术语漂移
- 提问：无新增知识库问题（本次以主线口述、纠偏和迁移题为主）
- 模块：`M07` 指令微调
- 学习内容：回顾“为什么少量高质量 `SFT` 数据也能明显改变行为”，继续固定“不是从零训练 + 高信号、高一致性监督 + 行为分布重塑”的统一口径
- 诊断表现：当前已能较稳说出 `SFT` 改变的主要是输出行为分布而不是凭空创造能力；后续仍需继续把“行为分布重塑”固定成最自然的标准用词
- 提问：无新增提问（本次为综合日中的短回顾）
- 模块：`M08` 人类反馈强化学习（RLHF）
- 学习内容：回顾“`RLHF-PPO` 里的 `KL penalty` 到底加在哪”以及“`reference model / reference policy / pi_old` 不是同一种约束”两道高频题
- 诊断表现：当前已能较稳说出 `KL penalty` 是先进入 reward shaping，再通过 `return / advantage` 间接影响 `clip loss`；也已能区分 `reference` 是分布锚点、`pi_old` 是单步更新稳定性约束。当前仍需继续压稳一点：把 `shaped reward -> return -> advantage -> clip loss` 的层次说得更顺，不要混成一团
- 提问：无新增提问（本次以到期高频口述为主）
- 模块：`M10` 解码策略
- 学习内容：完成 1 道“重复 + 偏长 + 降温后更模板化”的混合故障诊断，并额外梳理“重复的几种可能成因路径”
- 诊断表现：当前已能按“先分布、后约束”的顺序排查，知道先看 `temperature / top-p`，再看 `repetition penalty / stop sequence / max tokens`；同时已意识到“降温后重复更严重还是更缓解”取决于重复是由高概率循环还是分布过散导致。当前仍需继续压稳一点：把“重复成因分类 -> 对应调参顺序”压成更短面试口径
- 提问：无新增知识库问题（本次以场景诊断和纠偏为主）

## 2026-03-27
- 模块：`M14` Agent 与工具使用
- 学习内容：严格按 `Week 2 / Day 5` 推进 `M14`（下），先用 `Q084` 热身回顾 `continuous batching` 的最小调度粒度，再完成 `function calling / tool use / planning` 的 24 小时回顾；随后通过“总结文案不用工具、天气查询需要工具且会因 observation 分支、表格分析中 `tool selection` 属于 planning、表头缺失后要不要 re-plan、什么时候不该让模型自由选工具、为什么 schema follow 不是强 agent 的充分条件、为什么 agent latency 不只看 LLM latency”等场景题和口述题，正式完成“工具失败模式 / observation-action loop / re-plan 触发条件 / 系统边界”这一轮主线学习
- 诊断表现：当前已经能较稳说出 `function calling = 结构化调用意图`、`tool use = 真实工具执行`、`planning = 决定是否调用 / 调哪个 / 先后顺序 / observation 后是否改计划`，也已能区分 `re-plan` 的触发点不是“步骤多”，而是“新的 observation 让原计划前提失效”；同时已能指出 agent 常见失败不只是工具报错，还包括选错工具、误读 observation、不做纠偏、把调用成功当任务成功、越权执行。当前仍需继续压稳三点：一是“不该自由选工具”的判据要固定成“高风险 / 高成本 / 强权限约束”，不要回落成“模型不知道才别让它选”；二是 `schema follow` 不是充分条件要继续压缩成 2-3 句面试口径；三是 observation 异常后的“重试 / 换源 / 降级回复”还要再做 1 道迁移题
- 提问：无新增知识库问题（本次以主线场景题、口述纠偏与标准答案压缩为主）
- 模块：`M07` 指令微调
- 学习内容：按计划回顾“为什么少量高质量 `SFT` 数据也能明显改变行为”，重新收束“不是从零训练 + 高信号、高一致性监督 + 行为分布重塑”的统一口径
- 诊断表现：当前已能较稳说出 `SFT` 改变的主要是输出行为分布，而不是凭空增加很多新知识；后续仍需继续把“行为分布重塑”固定成标准用词，避免说成过泛的“输出分布变化”
- 提问：无新增提问（本次为到期回顾）

## 2026-03-26
- 模块：`M14` Agent 与工具使用
- 学习内容：按修正后的新 SOP 重新生成今日学习单，并严格对齐 `interview_4week_plan` 的 `Week 2 / Day 4`，正式启动 `M14`（上）；围绕 `function calling / tool use / planning` 的定义与边界，连续完成“为什么 `function calling` 改善的是能力调用方式而不是凭空创造能力”“为什么 schema follow 不是强 agent 的充分条件”“为什么 agent 上限更看 planning”“为什么很多 agent 失败败在 observe 后不会 re-plan”“什么时候该 tool use、什么时候不该调工具、为什么 tool selection 属于 planning”等一组核心判断题与场景题
- 诊断表现：当前已能较稳区分 `function calling = 结构化调用意图`、`tool use = 真实工具执行`、`planning = plan -> act -> observe -> re-plan` 的多步决策循环，也能把“强 agent 不等于会写 `tool_call JSON`”和“agent 体验不只取决于模型本身延迟”压成较短口径；当前仍需继续压稳三点：一是把“什么时候该 tool use、什么时候不该调工具”做成更自然的场景化判断；二是把 `planning` 和 `tool selection` 的关系说得更像 action selection，而不只是“为了完成目标服务”；三是继续进入 `M14`（下）的工具失败模式与 observation-action loop
- 提问：无新增知识库问题（本段以主线学习、判断题与场景题为主）
- 模块：`M12` 生产部署
- 学习内容：按计划回顾 `continuous batching / Paged KV`，重点纠正“continuous batching 的最小调度单位更接近 decode step / iteration，而不是整个 completion 任务”，并再次收束 `Paged KV` 的 block 管理与“管理优化 != 读带宽优化”
- 诊断表现：当前已能较稳说出 `continuous batching` 是 step 级动态补位，也能继续把 `Paged KV` 压成“优化 cache 管理，不直接消除长上下文下的带宽瓶颈”；当前仍需继续避免把 `continuous batching` 误说成“固定时间窗口 batching”，以及继续压稳 `TTFT / TPOT / tool latency` 在 agent 场景下的联动
- 提问：新增 `Q084`
- 模块：`M11` 提示工程进阶
- 学习内容：完成 24 小时回顾，重新口述“为什么 `few-shot` 常比抽象规则更有效”“为什么 prompt engineering 不能当万能药”“客服对话结构化抽取工程版 prompt 的设计顺序”
- 诊断表现：当前已能较稳把 `few-shot` 压成“直接暴露目标行为模式、减少歧义”，也能把 prompt engineering 压成“能力调用与行为约束，不是能力创造”；结构化抽取 prompt 的顺序已能回到 `schema -> 判定口径 -> few-shot -> 输出约束 -> 兜底规则`，当前仍需继续把字段歧义、schema 不匹配、程序化校验与回退策略收束为完整工程答案
- 提问：无新增提问（本次为到期回顾）
- 模块：`M08` 人类反馈强化学习（RLHF）
- 学习内容：完成到期快答，复述 `PPO vs DPO` 的共同目标与核心区别、`KL penalty` 的放置位置，以及 `reference model / reference policy / pi_old` 的分工
- 诊断表现：当前已能较稳回答 `PPO / DPO` 都是在 `SFT` 之后做偏好优化，`KL penalty` 是先并到 reward 侧形成 shaped reward，再通过 `return / advantage` 影响 `clip loss`；当前仍需继续纠正一点：不能把 `reference` 说成“防知识遗忘”，应稳定成“分布锚点”
- 提问：无新增提问（本次为到期快答）
- 模块：`M09` 高级对齐技术
- 学习内容：完成到期快答，回抽 `RLHF / RLAIF / Constitutional AI` 的关系，以及 `helpful / harmless / honest` 的张力
- 诊断表现：当前已能较稳按“反馈来源 / 显式原则 / 人类校准”三层解释三者关系，也能较稳回答 `honest` 不等于让用户满意；当前仍需继续把三者张力压成更自然、更对称的 2 句口径
- 提问：无新增提问（本次为到期快答）
- 模块：`M07` 指令微调
- 学习内容：完成到期快答，回顾“为什么少量高质量 `SFT` 数据也能明显改变行为”
- 诊断表现：当前已能较稳保持“不是从零训练 + 高信号、高一致性监督 + 行为分布重塑”的统一口径；后续仍需继续在场景题里保持“行为分布”这一表述不漂移
- 提问：无新增提问（本次为到期快答）
- 模块：`M05` 长文本建模
- 学习内容：完成到期快答，回顾 `RAG` 与原生长上下文的本质区别，以及“企业知识助手为什么不能统一只靠 `RAG`、也不能统一只靠长上下文”
- 诊断表现：当前已能较稳说出本质区别是“信息筛选发生在模型外还是模型内”，也能根据是否需要完整证据链、全文结构和成本延迟做场景化选型；当前仍需继续减少绝对化表述
- 提问：无新增提问（本次为到期快答）
- 模块：`M03` 分词与表示
- 学习内容：用 `Q082 / Q083` 做热身，回抽“tokenizer 为什么影响系统吞吐”“为什么 tokenizer 不是预处理细节”
- 诊断表现：当前已能较稳串起 `vocab size / 平均序列长度 / prefill / decode / TTFT / TPOT` 与系统吞吐之间的关系；后续仍需继续补 1 道估算题与 1 次长上下文迁移口述
- 提问：无新增提问（复用 `Q082 / Q083` 热身）

## 2026-03-25
- 模块：`M08` 人类反馈强化学习（RLHF）
- 学习内容：按学习模式先用 `Q080` 热身，再次压稳 `reward shaping / shaped reward` 的标准口径，重点纠正“不是 `reward model` 把整句 reward 变成训练序列，而是 `reward shaping` 在做这件事”
- 诊断表现：当前已能稳定说出 `shaped reward = 原始任务奖励 + 训练时加入的约束项`，并知道它先流向 `return / advantage`，再间接影响 `clip loss`；当前仍需继续避免把 `advantage` 简化成“reward 减平均收益”
- 提问：无新增提问（复用 `Q080` 热身）
- 模块：`M11` 提示工程进阶
- 学习内容：先用 `Q081` 回抽“为什么 `few-shot` 有时比抽象规则更有效”，随后完整推进“客服对话结构化抽取”prompt 实战：依次完成 schema 纠偏、`refund_request / escalation_needed` 判定规则、2 个边界 `few-shot`、工程版 prompt 骨架，以及“为什么始终返回合法 JSON + 兜底标签优于错误代码”的程序化兜底讨论
- 诊断表现：当前已能较稳把 `few-shot` 压成“直接暴露目标行为模式、减少歧义”，也已能按“标签口径 -> schema -> few-shot -> 输出约束 -> 兜底规则”设计结构化抽取 prompt；当前仍需继续压稳两点：一是把 `complaint_issue / refund_return / escalation_needed` 的边界讲得更自然，二是把“字段歧义 / schema 不匹配 / 程序化校验 / 回退”进一步压成标准工程答案
- 提问：无新增提问（本次以实战搭建与纠偏为主）
- 模块：`M09` 高级对齐技术
- 学习内容：继续收束“为什么 `helpful / harmless / honest` 有张力”以及“为什么 `honest` 不等于让用户满意”
- 诊断表现：当前已能把三者压成 4 句面试口径，也能说清 `honest` 的目标是尊重事实与能力边界，而不是取悦用户；当前仍需继续把三者的对称表达讲得更自然
- 提问：无新增提问（围绕既有 `M09` 口述题做短收束）
- 模块：`M03` 分词与表示
- 学习内容：正式从 `M11` 转入 `M03` 主线，连续完成“大 vocab / 短序列为什么都不能绝对化”“WordPiece vs Unigram”“tokenizer 为什么同时影响 prefill / decode”“`50k x 4096` embedding 参数量与 tied/untied”“BPE 为什么不是全局最优压缩”“代码 tokenizer 切分粒度权衡”“tokenizer 为什么影响系统吞吐、为什么在长上下文下影响更大”“为什么 tokenizer 不是预处理细节”等一组迁移题与估算题
- 诊断表现：当前已能较稳串起 `tokenizer -> vocab size / 平均序列长度 -> embedding/output 参数 -> prefill / decode -> TTFT / TPOT / token/s` 这条主线，也已能说清 `BPE` 是频次驱动的贪心 merge、`WordPiece` 更像似然导向训练准则、`Unigram` 更像显式概率模型。当前仍需继续压稳四点：一是 `prefill` 与 `decode` 的术语口径要继续避免口误；二是把“长度效应在长上下文下被放大”压得更短；三是补 1 轮 `M03 -> M12` 迁移口述；四是继续做 1 道 tokenizer 估算题
- 提问：新增 `Q082 / Q083`

## 2026-03-24
- 模块：`M08` 人类反馈强化学习（RLHF）
- 学习内容：按学习模式先用 `Q078` 热身回顾 `RLHF-PPO` 里 `KL penalty` 的位置，随后围绕“`reward shaping / shaped reward` 是什么”“`reference policy / reference model / pi_old` 各约束什么”“`PPO vs DPO` 的主线区别是什么”连续做口述纠偏
- 诊断表现：当前已能稳定说出 `KL penalty` 通常先并到 reward 侧做 shaping，再通过 `return / advantage` 进入 `clip loss`；也已能较稳区分 `reference` 负责“分布锚点”、`pi_old` 负责“单步更新约束”，并把 `PPO` 与 `DPO` 都定位为 `SFT` 之后的偏好优化。当前仍需继续压稳三点：一是别把 `advantage` 简化成“reward 减平均收益”；二是别把 `reference` 说成“防遗忘知识”；三是把 1 分钟总串讲再练到更自然
- 提问：新增 `Q080`
- 模块：`M09` 高级对齐技术
- 学习内容：完成“`RLHF / RLAIF / Constitutional AI` 的关系”和“为什么 `Constitutional AI` 更显式也不等于更客观、更不等于彻底解决对齐”的两轮口述收束
- 诊断表现：当前已能沿着“反馈来源 / 偏好标准表达方式 / 人类校准”三层主线解释 `RLHF / RLAIF / Constitutional AI`，也能较稳反驳“更显式 = 更客观 = 彻底解决对齐”。当前仍需继续补回 `helpful / harmless / honest` 的张力题，避免只压住主线、没压够张力题
- 提问：复用既有 `Q076 / Q077` 口述，无新增 `M09` 问题入库
- 模块：`M11` 提示工程进阶
- 学习内容：首次正式进入 `M11`，建立 `instruction design -> few-shot / decomposition -> 能力边界` 的基本框架，连续回答“为什么 `few-shot` 有时比一大段抽象规则更有效”“为什么 prompt engineering 不能当万能药”“结构化输出最常见失败模式是什么”，并完成 1 道客服对话结构化抽取任务的设计顺序题
- 诊断表现：当前已能较稳说出 `few-shot` 会直接暴露输入输出模式与决策风格，因此常比抽象规则更容易让模型 follow；也已理解 prompt engineering 更像“能力调用 + 行为约束 + 输出稳定化”，不能凭空创造模型没有的能力。结构化抽取任务中，已经能按“标签口径 -> schema -> few-shot”的顺序设计任务。当前仍需继续压稳两点：一是把“格式不合法 / schema 不匹配 / 内容越权 / 截断”与程序化校验、重试、回退一一对应；二是补 1 版完整 prompt 骨架到工程可用
- 提问：新增 `Q081`
- 模块：`M05` 长文本建模
- 学习内容：一句话快答回访“`RAG` 和原生长上下文的本质区别是什么”
- 诊断表现：已能稳定说出“信息筛选发生在模型外还是模型内”；当前仍需继续避免绝对化表述并答稳反向题意
- 提问：无新增提问（本次以收束快答为主）
- 模块：`M07` 指令微调
- 学习内容：一句话快答回访“为什么少量高质量 `SFT` 数据也能明显改变模型行为”
- 诊断表现：已能稳定说出“不是从零训练 + 高信号、高一致性监督 + 行为分布重塑”的主干；当前仍需继续把“输出分布”统一压准成“输出行为分布”
- 提问：无新增提问（本次以收束快答为主）
- 模块：`M12` 生产部署
- 学习内容：一句话快答回访“为什么长上下文会同时恶化 `TTFT` 和 `TPOT`”
- 诊断表现：已能较稳串起 `prefill`、`decode`、`KV cache` 与带宽压力的主线；当前仍需继续压稳 `TTFT / TPOT / token/s` 的统一口径
- 提问：无新增提问（本次以收束快答为主）
- 补充：夜间短会话继续按学习模式收束，先用 `Q080 / Q081` 做热身，再补 1 轮 `M09` 张力题，并启动“客服对话结构化抽取”prompt 骨架实战
- 模块：`M08` 人类反馈强化学习（RLHF）
- 学习内容：围绕 `Q080` 再做一次短口述，校准 `reward shaping / shaped reward -> return / advantage -> clip loss` 的链路
- 诊断表现：当前已知道 `KL penalty` 是先并到 reward 侧，再进入 `return / advantage / PPO`，但仍会偶尔把 `reward shaping` 误说成“把整句 reward 反向传给每个 token”；下次要继续把标准口径压成“原始任务奖励 + 训练时加入的约束项”
- 提问：无新增提问（复用 `Q080` 热身）
- 模块：`M09` 高级对齐技术
- 学习内容：补做“为什么 `helpful / harmless / honest` 往往有张力”的短口述
- 诊断表现：当前已能说出 `helpful` 与 `harmless` 的典型冲突，也能补上 `honest` 不等于让用户满意；当前仍需继续把三者张力压成更对称、更自然的 4 句面试口径
- 提问：无新增提问（围绕既有 `M09` 口述题做短纠偏）
- 模块：`M11` 提示工程进阶
- 学习内容：继续压稳“为什么 `few-shot` 常比抽象规则更有效”，并启动“客服对话结构化抽取”prompt 骨架题，先写任务描述、标签口径与输出 schema
- 诊断表现：当前已能把 `few-shot` 说到“直接暴露输入输出模式、减少歧义”这一层，也知道结构化抽取应先定标签口径再定 schema；但 prompt 骨架仍有三处待补：一是 schema 目前少于 4 个字段，二是 `refund_request` 的判定规则还不够清楚，三是还没补 2 个 `few-shot` 示例与失败兜底策略
- 提问：无新增提问（本次以实战骨架搭建与纠偏为主）

## 2026-03-23
- 模块：`M09` 高级对齐技术
- 学习内容：按学习模式先用 `Q076 / Q077` 热身复习“为什么更强的 `AI judge` / Constitutional AI 仍不能替代人类校准”“为什么 Constitutional AI 更显式也不等于更客观”，并继续把 `RLAIF / Constitutional AI` 压回 `RLHF / DPO` 这条偏好优化主线
- 诊断表现：当前已能较稳说出 `RLAIF` 是把部分人类反馈换成 `AI feedback`、`Constitutional AI` 是把原则显式化并驱动自我批评与修正，也能较稳区分“更显式 / 更可审查”与“更客观 / 彻底解决对齐”；当前仍需继续压稳两点：一是把 `M09` 全链路再压成 1 分钟自然口述，减少 `RLHF / RLAIF` 口误；二是继续把“显式原则不等于消除价值判断”说得更短
- 提问：复用 `Q076 / Q077` 热身，无新增 `M09` 问题入库
- 模块：`M08` 人类反馈强化学习（RLHF）
- 学习内容：因主动提出“`PPO / DPO` 细节忘得差不多了”，临时切到重学模式，系统重建 `SFT -> 偏好优化` 主线，细讲 `PPO` 的 `reward model + value model + KL penalty + advantage + clip loss` 链路、`reference model` 与 `pi_old` 的分工、`DPO` 的隐式奖励、`beta`、长度偏置，以及为什么 `DPO` 更吃偏好数据质量；最后完成 1 次 `PPO vs DPO` 总口述
- 诊断表现：当前已能较稳说出 `PPO` 与 `DPO` 都是在 `SFT` 后做偏好优化，前者是显式 `Reward Model + RL` 路线、后者是直接偏好优化路线；也已理解 `KL penalty` 通常先在 reward 侧做 shaping，再通过 `return / advantage` 进入 `clip loss`。当前仍需继续压稳四点：一是不要把 `value model` 误说成“由偏好数据直接训练”；二是把 `reference model / reference policy / pi_old` 的分工说得更自然；三是继续压稳 `beta` 是“偏好信号相对 `reference` 约束强弱”而不只是“放大 loss”；四是把 `DPO` 的长度偏置稳定表述为“长度污染真实偏好信号”，避免简化成“天然偏短”
- 提问：见 `Q078 / Q079`
- 模块：`M05` 长文本建模
- 学习内容：一句话快答回访“`RAG` 和原生长上下文的本质区别是什么”
- 诊断表现：已能稳定说出“信息筛选发生在模型外还是模型内”；当前仍需继续避免绝对化表述并答稳反向题意
- 提问：无新增提问（本次以收束快答为主）
- 模块：`M07` 指令微调
- 学习内容：一句话快答回访“为什么少量高质量 `SFT` 数据也能明显改变模型行为”
- 诊断表现：已能稳定说出“不是从零训练 + 高信号、高一致性监督 + 行为分布重塑”的主干；当前仍需继续把“输出分布”统一压准成“输出行为分布”
- 提问：无新增提问（本次以收束快答为主）
- 模块：`M12` 生产部署
- 学习内容：一句话快答回访“为什么长上下文会同时恶化 `TTFT` 和 `TPOT`”
- 诊断表现：已能较稳串起 `prefill`、`decode`、`KV cache` 与带宽压力的主线；当前仍需继续压稳 `TTFT / TPOT / token/s` 的统一口径
- 提问：无新增提问（本次以收束快答为主）

## 2026-03-22
- 模块：`M09` 高级对齐技术
- 学习内容：按学习模式完成 `Q074 / Q075` 热身复习，随后连续回答“`RLHF / RLAIF / Constitutional AI` 的关系”“为什么 `helpful / harmless / honest` 往往存在张力”“为什么即使 `AI judge` 很强也不能替代人类校准”“为什么 `Constitutional AI` 更显式也不等于更客观、更不等于彻底解决对齐问题”，并完成 1 轮面试化场景串讲与 1 次 1 分钟总串讲收束
- 诊断表现：当前已能较稳说出 `SFT` 更擅长学习目标格式、风格和基本行为分布，偏好对齐更擅长学习多个可答方案里的相对偏好与取舍；也已能把 `RLHF / RLAIF / Constitutional AI` 串成“人类反馈 / AI feedback / 显式原则驱动的自我批评与修正”，并明确 `AI judge` 不能替代人类校准，因为原则和优先级本身是人定的、judge 偏差会被系统性放大。当前仍需继续压稳三点：一是 1 分钟总串讲里减少 `RLHF / RLAIF` 口误和“R/L 阶段”这类不准口径；二是把 `Constitutional AI` 稳定表述为“更显式、更可审查”而不是“更客观、彻底解决”；三是把 `helpful / harmless / honest` 压成更短的面试答案
- 提问：见 `Q077`（本次其余主要为围绕既有 `M09` 题目的口述与迁移追问）
- 模块：`M05` 长文本建模
- 学习内容：一句话快答回访“为什么不能统一只用 `RAG`，也不能统一只用长上下文”
- 诊断表现：已能稳定说出两者本质区别是“信息筛选在模型外还是模型内”，当前仍需继续避免绝对化表述并答稳反向题意
- 提问：无新增提问（本次以收束快答为主）
- 模块：`M07` 指令微调
- 学习内容：一句话快答回访“为什么少量高质量 `SFT` 数据也能明显改变模型行为”
- 诊断表现：已能说出“不是从零训练 + 高信号、高一致性监督 + 行为分布重塑”的主干，当前仍需继续把“输出分布”统一压准成“输出行为分布”
- 提问：无新增提问（本次以收束快答为主）
- 模块：`M12` 生产部署
- 学习内容：一句话快答回访“为什么长上下文会同时恶化 `TTFT` 和 `TPOT`”
- 诊断表现：已能较稳串起 `prefill`、`decode`、`KV cache` 与带宽压力的主线；当前仍需继续压稳 `TTFT / TPOT / token/s` 的统一口径
- 提问：无新增提问（本次以收束快答为主）

## 2026-03-21
- 模块：`M05` 长文本建模
- 学习内容：用 `Q036` 热身回顾 `RoPE` 如何通过绝对旋转表达相对位置，并连续回答“为什么不能统一只用 `RAG`”“为什么不能统一只用长上下文”两道反向追问题
- 诊断表现：已能较稳说出 `RAG` 与原生长上下文的本质区别是“信息筛选发生在模型外还是模型内”，也能把系统成本、信息筛选与任务结构串起来；当前仍需继续避免“无法胜任”“一定更好”这类绝对化表述
- 提问：无新增提问（本次以快答复习与反向追问为主）
- 模块：`M07` 指令微调
- 学习内容：回顾“为什么少量高质量 `SFT` 数据也能明显改变行为”“为什么不能只把副作用总结成遗忘”，并继续把答案压成 2 句面试口径
- 诊断表现：已能较稳说出 `SFT` 不是从零训练，而是在已有预训练能力上用窄分布、高信号监督重塑默认行为分布；当前主要薄点是不时会把“高信号、高一致性监督”说成“高信息密度”
- 提问：无新增提问（本次以短口述纠偏为主）
- 模块：`M12` 生产部署
- 学习内容：回顾“为什么长上下文会同时恶化 `TTFT` 与 `TPOT`”，继续压稳 `prefill / decode / KV cache / Paged KV` 的系统链路
- 诊断表现：已能较稳解释 `TTFT` 看 `prefill`、`TPOT` 看 decode 时读取历史 `KV cache` 的带宽压力，也知道 `Paged KV` 只优化管理而不直接消除带宽成本；当前仍需继续压准 `prefill` 等术语
- 提问：无新增提问（本次以系统链路口述与纠偏为主）
- 模块：`M10` 解码策略
- 学习内容：快问快答回访 beam search 在开放式生成中的边界，以及为什么解码策略改变表现而不改变能力
- 诊断表现：已能较稳说出 beam search 和开放式生成目标有错位；“改变表现不改变能力”已懂主干，但仍需继续避免说成“不改变模型输出”
- 提问：无新增提问（本次以 2 句快答回访为主）
- 模块：`M01` Transformer 架构深度解析
- 学习内容：短口述回访 `pre-norm / RMSNorm` 为什么是现代 LLM 更常见的默认基础形态
- 诊断表现：已能说出 `pre-norm` 更好保住干净残差路径、`RMSNorm` 少了减均值但仍保留尺度控制；当前仍需继续提升术语自然度
- 提问：无新增提问（本次以 2 句快答回访为主）
- 模块：`M03` 分词与表示
- 学习内容：短口述回访 `WordPiece`“似然导向训练准则”与 `Unigram`“显式概率模型”的区别
- 诊断表现：已能说清两者主干差异；当前仍需继续压稳“同一句话可有多种切分且各自有概率”这层口径
- 提问：无新增提问（本次以 2 句快答回访为主）
- 模块：`M09` 高级对齐技术
- 学习内容：首次正式进入 `M09` 主线，连续学习“为什么 `SFT` 之后还需要偏好对齐”“为什么不能只靠更大规模 `SFT` 替代偏好对齐”“`RLHF / RLAIF / Constitutional AI` 的关系”“为什么 `helpful / harmless / honest` 存在张力”“为什么规则化 `AI feedback` 容易让模型更保守”“为什么更强的 `AI judge` 仍不能完全替代人类校准”
- 诊断表现：当前已能把 `预训练 -> SFT -> 偏好对齐` 这条链路串起来，知道偏好对齐更像是在多个能答方案里选更可取的那个；也已能说出 `RLAIF` 与 `Constitutional AI` 同属 `AI feedback` 路线，后者更强调显式原则与可解释性。当前仍需继续压稳三点：一是把 `helpful / harmless / honest` 的冲突补完整，避免只说前两者；二是把“AI feedback / 显式原则 / 人类校准”的三层结构说得更短；三是继续用迁移题压稳“规则化反馈为什么会把模型推向保守”
- 提问：见 `Q074-Q076`
- 补充：本次会话中已显式把“新问题”与“复习题”分开梳理；后续若单场混合推进新学与复习，开场应先标注两条线，再进入具体问题

## 2026-03-19
- 模块：`M01` Transformer 架构深度解析
- 学习内容：复习“残差连接 -> pre-norm -> RMSNorm”这条主线，重点校准“残差连接不只是防梯度消失，而是提供更接近恒等映射的通路 + 学增量修正”“pre-norm 更好保住干净的残差主干”“RMSNorm 的标准口径是按均方根缩放、不做减均值”
- 诊断表现：已能说主干，知道 pre-norm 更稳与残差主干干净有关；当前仍需继续压稳两点：一是不要把残差作用只简化成“防梯度消失”，二是不要把 RMSNorm 的计算口径说成“平方和开根号”
- 提问：无新增提问（本次以口述校准与纠偏为主）
- 模块：`M03` 分词与表示
- 学习内容：复习 `WordPiece`“似然导向训练准则”与 `Unigram`“显式概率模型”的差异，并补稳“一句话可有多种切分且各自有概率”的口径
- 诊断表现：已能说清 WordPiece 更像用似然收益指导 merge、Unigram 更像显式给不同切分分配概率；当前仍需继续防止把 Unigram 说成“确定性概率模型”
- 提问：无新增提问（本次以短口述复盘为主）
- 模块：`M05` 长文本建模
- 学习内容：按 2 小时正式节奏进入上半部分主线，系统讲解“为什么长文本建模不只是位置编码问题”，覆盖系统层 / 表示层 / 方案层三层框架、`RoPE/位置外推` 的边界、`原生长上下文 / RAG / 压缩记忆 / 稀疏注意力` 四条路线的吸引力与代价，以及企业知识库问答、长合同审阅两个场景的选型直觉
- 诊断表现：已能较稳说出 `RoPE/位置外推` 主要解决“能不能处理更长位置”，不保证“能不能有效利用长上下文”；已能区分 `RAG`“模型外先筛再给证据”与原生长上下文“把原材料整体交给模型自己处理”；已能说明统一方案的代价，但仍需继续减少绝对化表述
- 提问：无新增提问（本次以主线推进、迁移题与口述训练为主）
- 模块：`M10` 解码策略
- 学习内容：补做“重复 + 偏长 + 降温后更模板化”的混合故障诊断，重新压稳 `temperature / top-p / repetition penalty / max tokens / stop sequence` 的职责边界
- 诊断表现：已能说出“先分布、后约束”的排查顺序，知道不能只靠继续降 `temperature` 解决重复问题；当前仍需继续压稳 `top-p` 作为候选集边界控制项的口径
- 提问：无新增提问（本次以诊断题与标准口径收束为主）
- 模块：`M12` 生产部署
- 学习内容：抽查“为什么 `KV cache` 只缓存历史 `K/V` 而不缓存历史 `Q`”，补稳 decode 时“新 token 生成新 `Q/K/V`、历史 `K/V` 被反复读取、历史 `Q` 不再复用”的读写路径
- 诊断表现：已能较稳口述缓存机制的主干；当前仍适合继续与 `TTFT / TPOT / token/s / Paged KV` 一起串讲
- 提问：无新增提问（本次以短复习口述为主）
- 补充：按项目规则修正了“学习模式下默认今日学习单”的执行口径；若学习者未明确说明时间不足，后续默认按 2 小时完整版安排
- 模块：`M05` 长文本建模
- 学习内容：加餐继续下钻 `RoPE` 主线，补讲 `RoPE` 如何通过对不同维度对的 `Q/K` 做不同频率旋转来表达相对位置、为什么高频/低频会有不同作用，以及为什么外推常表现为“长度能拉长，但远距离质量不一定同步提升”；同时补了位置缩放 / 插值“用位置分辨率换可外推长度”的工程直觉，并继续做企业知识库问答 vs 长合同审阅、统一都用 `RAG` 的代价等选型题
- 诊断表现：已能较稳口述 `RoPE` 通过“绝对旋转 -> 内积只依赖相对位置差”的主干，也已能解释“高频更易混叠、低频更稳但分辨率更粗”；当前仍需继续压稳两点：一是把 `RoPE` 相对位置表达的解释压成更短的面试口径，二是继续避免把 `RAG` / 原生长上下文说成绝对优劣
- 提问：见 `Q036`
- 模块：`M07` 指令微调
- 学习内容：重新启动 `SFT` 主线，补讲“为什么少量高质量 SFT 数据也能明显改变行为”“为什么 SFT 的副作用更准确地说来自窄分布 + 强监督约束，而不只是遗忘”
- 诊断表现：已能说清 `SFT` 不是从零训练，而是在已有预训练能力上用高信号监督重塑行为分布；当前仍需继续压稳两点：一是把“高信号监督 + 行为分布重塑”压成标准答案，二是把“遗忘不是全部，行为偏置才是更核心机制”说得更完整
- 提问：见 `Q037`

## 2026-03-20
- 模块：`M05` 长文本建模
- 学习内容：按 2 小时正式节奏继续推进主线，围绕“为什么长文本建模不只是位置编码问题”“为什么不能统一都用 `RAG`”“企业知识库助手如何做 `RAG + 原生长上下文` 的混合选型”连续完成口述、场景题与面试追问；同时再次收束 `RAG` 与原生长上下文的本质区别，并复述“RoPE 外推不等于长文本系统做好了”
- 诊断表现：当前已能较稳从系统层 / 表示层 / 方案层解释长文本问题，也已能在选型题中完成“局部问答默认 `RAG`、跨页条款与完整证据链升级原生长上下文”的混合路由；仍需继续压稳两点：一是遇到“统一都上长上下文”与“统一都上 `RAG`”这类反向追问时避免答反题意，二是继续减少绝对化表述
- 提问：无新增提问（本次以口述训练、场景题和反向追问为主）
- 模块：`M07` 指令微调
- 学习内容：围绕“为什么少量高质量 `SFT` 数据也能明显改变行为”“为什么不能只把副作用总结成遗忘”“为什么不能把 `SFT` 理解成只是套了一层助手模板”做多轮 2 句口述与快问快答
- 诊断表现：已能较稳说出 `SFT` 不是从零训练，而是在已有预训练能力上用窄分布、高信号监督重塑行为分布；也能说出模板化、保守和分布外变钝更像是行为偏好被重新塑形，而不只是知识被遗忘。当前仍需继续把“高信号监督 + 目标行为格式 + 行为分布重塑”压成更短、更自然的面试口径
- 提问：无新增提问（本次以快问快答与标准答案收束为主）
- 模块：`M12` 生产部署
- 学习内容：抽查 `TTFT / TPOT / token/s` 的指标分工、`KV cache` 为什么只缓存历史 `K/V` 不缓存历史 `Q`、`Paged KV` 的 block table 作用，以及“长上下文为什么会同时恶化 TTFT 和 TPOT”的系统因果链
- 诊断表现：已能较稳串起 `prefill`、`KV cache`、带宽、`Paged KV` 四者之间的关系，也能清楚解释 `Q` 只在当前 step 使用而历史 `K/V` 会被反复读取；当前仍需继续压稳两点：一是 `token/s` 与 `TPOT` 的关系，二是 `Paged KV` 优化的是 cache 管理，而不直接消除长上下文带来的带宽成本
- 提问：无新增提问（本次以系统链路口述与纠偏为主）
- 模块：`M10` 解码策略
- 学习内容：快问快答抽查 beam search 的任务边界、解码策略为什么改变表现不改变能力，以及开放式生成默认参数的口述稳定性
- 诊断表现：已能较稳回答 beam search 为什么不适合作为开放式生成默认方案；“解码策略改变表现、不改变能力”已理解主干，但仍需避免说成“不改变模型输出”
- 提问：无新增提问（本次以快问快答追问为主）
- 模块：`M01` Transformer 架构深度解析
- 学习内容：短口述回访 `pre-norm / RMSNorm / 残差主干` 的标准口径
- 诊断表现：已能说主干：`pre-norm` 更好保住残差恒等路径、`RMSNorm` 少了减均值步骤但仍能提供尺度稳定性；当前仍需继续提升自然度，避免只说“norm 在层内”
- 提问：无新增提问（本次以 2 句快答回访为主）
- 模块：`M03` 分词与表示
- 学习内容：再次口述 `WordPiece`“似然导向训练准则”与 `Unigram`“显式概率模型”的标准差异，并纠正“不能只说一个 merge、一个删词”
- 诊断表现：当前已能较稳说出 `WordPiece` 用似然收益指导 merge、`Unigram` 显式给 token 和切分分配概率；仍需继续保持“训练准则 vs 显式概率模型”这层抽象，不回落到过程表象
- 提问：无新增提问（本次以短口述纠偏为主）

## 2026-03-18
- 模块：`M10` 解码策略
- 学习内容：两道混合故障诊断题与 1 道总收束题，覆盖“重复 + 偏长但不跑题”时先看 `repetition penalty`、“去模板化后变脏”时先收 `top-p`，以及“任务目标而不是产品形态决定默认解码风格”
- 诊断表现：已能区分“重复/啰嗦”与“尾部脏/跑偏”两类场景，知道前者优先看 `repetition penalty`、后者优先收 `top-p`；当前仍需继续压稳“为什么不是先动 temperature”的面试化解释
- 提问：无新增提问（本次以诊断题和标准口径收束为主）
- 模块：`M03` 分词与表示
- 学习内容：WordPiece“似然导向训练准则”与 Unigram“显式概率模型”短口述复盘、tokenizer 对 prefill / decode 双重影响复述、`50k/100k vocab` 权衡题
- 诊断表现：已能稳定说出“prefill 更看序列长度，decode 还要看输出层 / 词表大小 / 带宽”；能判断大词表短序列不必然整体更优；当前仍需继续压稳两点：一是 Unigram 的“同一句话可有多种切分且各自有概率”，二是 WordPiece 的“似然导向训练准则”口径自然度
- 提问：无新增提问（本次以短复习口述与迁移题为主）
- 模块：`M05` 长文本建模
- 学习内容：系统层 / 表示层 / 方案层三层框架短复述，补强“原生长上下文”路线与统一方案代价
- 诊断表现：已能完整带出 `原生长上下文 / RAG / 压缩记忆 / 稀疏注意力` 四条路线，并能说明“位置外推解决能不能放进去，不等于能不能有效利用”；当前仍需继续避免绝对化表述
- 提问：无新增提问（本次以口述复盘与纠偏为主）
- 模块：`M12` 生产部署
- 学习内容：`KV cache` 为什么不缓存历史 `Q`、`TTFT / TPOT / token/s` 的指标分工短复习
- 诊断表现：已能说清历史 `Q` 不会被后续 step 复用、历史 `K/V` 会被反复读取；当前仍需继续压稳 `TPOT` 与系统整体 `token/s` 的区分
- 提问：无新增提问（本次以短复习口述为主）
- 模块：`M01` Transformer 架构深度解析
- 学习内容：提问模式补强 `pre-norm / post-norm / RMSNorm / 残差连接` 这条主线，集中回答当前主流为什么偏 pre-norm、为什么现代 LLM 常用 RMSNorm、残差连接为什么让深层网络更稳定，以及为什么口头上仍会说 `pre-LN / post-LN`
- 诊断表现：已能把“残差连接提供恒等路径与增量修正 -> pre-norm 更好保住这条主干 -> RMSNorm 用更简单的 norm 保留控尺度收益”串成一条因果链；当前仍需继续压稳三点：一是不要把残差作用只简化成“防梯度消失”，二是不要把 RMSNorm 直接说成经典 LayerNorm，三是 `pre-LN / post-LN` 更准确是在说 `pre-norm / post-norm` 的结构位置
- 提问：Q033-Q035

## 2026-03-17
- 模块：`M10` 解码策略
- 学习内容：失败模式与调参顺序第二轮、重复/过长/发散/模板化的排查框架、stop sequence vs max tokens、temperature 与 top-p 的分工、聊天/代码/摘要/事实问答四类任务的默认解码风格
- 诊断表现：已能稳定说出“temperature 控制探索强度，top-p 控制探索边界”；能区分 stop sequence 的边界触发与 max tokens 的长度硬上限；能按任务目标解释四类任务的默认解码风格。当前仍需继续压稳“重复 + 过长但不跑题”时先动 repetition penalty 而不是先动温度
- 提问：无新增提问（本次以诊断题口述与纠偏为主）
- 模块：`M03` 分词与表示
- 学习内容：WordPiece“似然导向训练准则”与 Unigram“显式概率模型”短复习、tokenizer 对 prefill / decode 成本影响的标准口径
- 诊断表现：已能说清 prefill 看序列长度、decode 看输出层 / 词表大小 / 带宽；WordPiece 与 Unigram 的主干差异已通，但标准表述自然度仍需再压 1 次
- 提问：无新增提问（本次以短复习口述为主）
- 模块：`M05` 长文本建模
- 学习内容：系统层 / 表示层 / 方案层三层框架，`RAG` vs 原生长上下文的本质区别，原生扩窗 / 稀疏注意力 / 压缩记忆 / `RAG` 四条路线的失败模式，企业知识库问答与超长合同审阅两个场景的选型训练，120 字费曼收束
- 诊断表现：已能说清长文本问题不只是位置编码问题，而是系统成本、表示外推和方案权衡的叠加；能区分 `RAG` 的“先筛再读”与长上下文的“少筛直接读”；当前仍需继续压稳两点：一是回答中少用绝对化表述，二是“如果只能给一个统一方案，会牺牲什么”的面试口径
- 提问：无新增提问（本次以临时插学、口述训练与纠偏为主）

## 2026-03-16
- 模块：`M10` 解码策略
- 学习内容：greedy / temperature / top-k / top-p、beam search 与 length penalty、repetition penalty / stop sequence / max tokens、beam 与 sampling 的任务边界、失败模式与调参、按任务反推默认参数
- 诊断表现：已能解释“模型给分布，解码把分布变成输出”；能区分采样控制项和工程约束项；能说明 beam search 不只是更贵，也更偏保守与模板化；已建立重复/发散/过短/过保守的第一轮调参映射；当前仍需继续压稳“多现象并存时的排查顺序”与聊天/代码/摘要的参数选型直觉
- 提问：Q045-Q062
- 模块：`M10` 解码策略
- 学习内容：beam search 为什么不适合开放式生成、重复/模板化/过长场景下的排查顺序、temperature 调高后与 top-p 的联动
- 诊断表现：已能口述“更贵 + 更保守 + 更模板化”的 beam 边界，也能在“先保留多样性，再收尾部风险”的框架下解释 temperature 与 top-p 的联动；下一步需要继续训练多参数同时变化时的排查顺序
- 提问：Q063-Q066
- 模块：`M03` 分词与表示
- 学习内容：BPE / WordPiece / Unigram 对比复习、WordPiece 中“似然收益”与概率口径、Unigram 的显式概率模型直觉、SentencePiece 的层次边界
- 诊断表现：三者主干差异已能口述；WordPiece vs Unigram 的区别明显更清楚；当前仍需继续压稳“概率、条件概率、似然”三者的层次，以及为什么 WordPiece 不应被想成每步都训练完整语言模型
- 提问：Q067-Q073
- 模块：`M07` 指令微调
- 学习内容：SFT 副作用根源短复习
- 诊断表现：仍需继续压稳“窄分布 + 强监督约束”，避免简化成“继续训练导致遗忘”
- 提问：无新增提问

## 2026-03-15
- 模块：`M07` 指令微调
- 学习内容：少量高质量 SFT 数据为什么有效、SFT 副作用与根源、LoRA 的显存收益与低秩边界
- 诊断表现：已能说明 SFT 不是从零训练、主要改变行为分布；LoRA 省显存的主因较稳；SFT 副作用的根源仍容易简化成“继续训练导致遗忘”，还需压稳“窄分布 + 强监督约束”
- 提问：无新增提问（本次以口述复测为主）
- 模块：`M03` 分词与表示
- 学习内容：大 vocab 与短序列的权衡、tokenizer 为什么同时影响 prefill 与 decode、BPE/WordPiece/Unigram 的训练目标差异
- 诊断表现：已能把“prefill 看序列长度、decode 看词表大小”讲清；BPE 与 Unigram 基本稳定；WordPiece 的“似然收益驱动”表述还需继续压稳
- 提问：无新增提问（本次以口述复测为主）
- 补充：完成了 1 次“3 分钟快复盘”，将昨日主线收束为 4 句记忆钩子

## 2026-03-12
- 模块：`M07` 指令微调
- 学习内容：SFT 改变行为分布、少量高质量数据为何有效、SFT 副作用、LoRA 低秩增量、客服助手 SFT 数据样例设计
- 诊断表现：已能区分“行为变化”与“知识增加”；能设计客服场景下的澄清、拒答、负面情绪样本；SFT 副作用的口述还可再压稳
- 提问：Q033-Q035
- 模块：`M03` 分词与表示
- 学习内容：subword trade-off、BPE/WordPiece/Unigram、embedding 参数量、tied embeddings、tokenizer 对 serving 成本的影响、中英混合代码 tokenizer 设计维度
- 诊断表现：能把 vocab size、序列长度、prefill/decode、KV cache 串起来；WordPiece 的目标表述仍需再压稳
- 提问：Q030-Q032
- 模块：`M12-01` 推理优化（Prefill vs Decode）
- 学习内容：prefill/decode 复杂度、KV cache、continuous batching、Paged KV、chunked prefill、speculative decoding、KV 量化
- 诊断表现：能解释 prefill/decode 不同瓶颈；能估算 batching 利用率；能区分 PyTorch batch 与 kernel 调度 batch
- 提问：Q018-Q029

## 2026-03-11
- 模块：`M08-01` DPO 稳定性与偏差分析
- 学习内容：beta 取值、长度偏置、数据构造优先级、偏短/啰嗦问题
- 诊断表现：能提出长度匹配和信息密度对比两条数据构造规则
- 提问：Q017

## 2026-03-10
- 模块：`M08-01` RLHF 基础
- 学习内容：SFT → RM → PPO/DPO、Bradley-Terry、Advantage、PPO 训练循环、DPO 原理与练习
- 诊断表现：PPO 四模型、DPO 隐式奖励、margin 边界理解稳定
- 提问：Q011-Q016

## 2026-03-07
- 模块：`M01-03`、`M02-01`、`M06-01`
- 学习内容：位置编码、预训练基础、分布式训练与 ZeRO
- 诊断表现：RoPE/ALiBi、AdamW、ZeRO 三阶段关系掌握良好
- 提问：Q006-Q010

## 2026-03-06
- 模块：`M01-01`、`M01-05`、`M04`、`M01-06`
- 学习内容：Attention 回顾、Pre-LN/Post-LN、MQA/GQA、FlashAttention、Mini-GPT 实战引入
- 诊断表现：Attention 公式与多头计算量有过易错点；位置编码与复杂度分析表现较好
- 提问：Q001-Q005
