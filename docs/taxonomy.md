# 知识点划分与命名规范（统一口径）

> 目的：解决 README / curriculum / progress 中“模块编号”口径不一致导致的歧义。

## 1) 统一规则（从现在开始默认使用）

### 1.1 唯一的“课程模块（最外围）”编号：`M01` ~ `M16`
- **`Mxx` 只表示 README 里的 16 个模块**（最外围知识点）。
- 以后对话里你说的“`M3` / `M03`”，默认就是 **`M03: 分词与表示`**。

### 1.2 模块内子主题编号：`M03-01` / `M03-02` …
- 例：`M03-01 Tokenization`，`M03-02 Embedding`
- 例：`M10-03 Speculative Decoding`

### 1.3 其它文档 / 扩展主题的编号不再用 `Mxx`
为避免冲突，三个“非主线模块”前缀使用不同语义：
- `Axx`：`curriculum/modules_outline.md` 的 **8 个宏模块**（Macro Outline）
- `Sxx`：`curriculum/syllabus.md` 的 **16 个学习路径模块**（Syllabus Track）
- `Exx`：`Week 4+` 延长补强使用的 **6 个扩展主题**（Extension Topics）

> 结论：**`Mxx` 只有一套主线模块**；`Axx`、`Sxx`、`Exx` 只是不同视图或扩展层，不与 `Mxx` 抢编号。

### 1.4 模块内知识点标签规范
每个模块内的知识点统一使用以下标签，便于决定讲解深度与复习优先级：
- `核心必会`：不掌握会直接影响后续模块理解。
- `面试高频`：面试中高概率直接提问或变体追问。
- `工程重点`：与系统实现、性能、资源估算或调参直接相关。
- `扩展阅读`：不影响主线推进，但有助于拓展视野或研究深度。

### 1.5 模块收录下限
每个 `Mxx` 模块至少包含：
- 1 组概念与原理的最小闭环。
- 1 组易错点或边界条件。
- 1 个工程联系（估算题、实现题、设计题三选一）。
- 1 个面试高频问法与标准回答骨架。
- 明确的前置依赖与后续关联。

---

## 2) Canonical Mainline：`M01` ~ `M16`（与 README 对齐）

| ID | 模块 |
|---:|------|
| M01 | Transformer 架构深度解析 |
| M02 | 预训练理论与方法 |
| M03 | 分词与表示 |
| M04 | 高效注意力变体 |
| M05 | 长文本建模 |
| M06 | 模型并行与效率 |
| M07 | 指令微调 (Instruction Tuning) |
| M08 | 人类反馈强化学习 (RLHF) |
| M09 | 高级对齐技术 |
| M10 | 解码策略 |
| M11 | 提示工程进阶 |
| M12 | 生产部署 |
| M13 | 多模态 LLM |
| M14 | Agent 与工具使用 |
| M15 | 模型可解释性 |
| M16 | 测试时计算扩展 (o1-like) |

---

## 3) Extension：`E01` ~ `E06`（Week 4+ 延长补强）

> `Exx` 是主线 `M01` ~ `M16` 之外的扩展主题，用于延长学习周期时补齐横切知识点。

| ID | 扩展主题 |
|---:|----------|
| E01 | 评估与基准测试（Evaluation & Benchmarking） |
| E02 | 安全与红队（Safety & Red Teaming） |
| E03 | 数据工程（Data Engineering） |
| E04 | RAG 与检索（RAG & Retrieval） |
| E05 | 量化与服务栈（Quantization & Serving Stack） |
| E06 | 现代架构扩展（MoE / SSM / Mamba） |

使用规则：
- `E01` ~ `E06` 默认不插入当前进行中的 `Week 1 ~ Week 4` 主线。
- 若后续对话里提到“`E01` / `E02` ...”，默认就是对应扩展主题。
- 进度、提问归档、延长期计划里，引用这些主题时统一使用 `Exx`。

---

## 4) 兼容：历史学习记录（Legacy → Canonical）

早期文档里出现过 `M1-xx / M2-xx` 这套“会话内自定义编号”。为了统一检索与引用，后续会在文档里按下表迁移到 canonical（必要时保留旧标记做括号别名）。

| Legacy | Canonical | 说明 |
|---|---|---|
| M1-01 Attention机制回顾 | M01-01 | Transformer / Attention |
| M1-02 位置编码深度解析 | M01-03 | Transformer / Position |
| M1-03 LayerNorm与架构演进 | M01-05 | Transformer / LN+Residual |
| M1-04 高效Attention变体 | M04 | 对齐 README 的模块划分 |
| M1-05 从0实现Mini-GPT | M01-06 | 归入“代码实战”子主题（README 会补 1.6） |
| M2-01 预训练基础 | M02-01 | Pretraining |
| M2-02 分布式训练 | M06-01 | 并行与效率 |
| M2-03 RLHF基础 | M08-01 | RLHF |
| M2-04 推理优化 | M12-01 | 部署/推理优化（含 KV、batching 等） |

---

## 5) `Axx`（8 宏模块）与 `Mxx` / `Exx` 的映射（粗粒度）

> `Axx` 只是“按阶段打包”的视图，便于规划周期；学习与记录仍用 `Mxx`。

| Axx | 宏模块 | 覆盖的 canonical 模块（示例） |
|---:|---|---|
| A01 | Transformer 深度剖析 | M01 |
| A02 | 预训练技术与范式 | M02, M03, M06, M08(部分) |
| A03 | 对齐技术 | M07, M08, M09 |
| A04 | 推理与部署工程 | M10, M12, M05(部分) |
| A05 | 现代架构演进 | M04(部分), M05(部分), M13, M14 |
| A06 | 评估与安全 | E01, E02, M09, M15(部分) |
| A07 | 前沿研究与趋势 | M16, E06, M15(部分) |
| A08 | 综合实战项目 | （项目型，不强行映射到单一 `Mxx`） |

---

## 6) `Sxx`（Syllabus Track）说明

`curriculum/syllabus.md` 里的 `Module 01..16` 是另一条“从基础到前沿”的教学叙事线，不与 README 的 `M01..M16` 一一对应。

建议做法：
- 规划学习路径可以参考 `Sxx`
- **主线记录进度 / 提问归档 / 会话标签统一用 `Mxx`**
- 延长期和补强主题统一用 `Exx`
